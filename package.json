{
  "name": "cloud-customer-portal",
  "private": true,
  "workspaces": [
    "portal",
    "packages/email-kit"
  ],
  "scripts": {
    "dev": "npm --prefix portal run dev",
    "build": "npm --prefix portal run build",
    "setup": "npm --prefix portal run setup",
    "install:all": "npm install && npm --prefix portal install"
  },
  "devDependencies": {
    "ts-node": "^10.9.2"
  }
}
